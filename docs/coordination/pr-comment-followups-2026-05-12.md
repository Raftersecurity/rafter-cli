# PR Comment Follow-ups — 2026-05-12

Generated by: raftercli/polecats/opal  
Bead: sable-0q1 (B6 — Get agents on long-tail of Rome-commented Rafter CLI PRs)

## Open PRs Scanned

All open PRs on Raftersecurity/rafter-cli as of 2026-05-12:

| PR | Title | Rome Comment? |
|----|-------|---------------|
| #95 | feat(promo): agent-only 60s video production pipeline (rf-zg8z) | ✅ Yes |
| #83 | docs: 60s Rafter promo video storyboard (se-p3r) | ❌ No |
| #82 | [DRAFT] feat(experimental): prompt-injection detector (rf-bmo) | ❌ No |
| #75 | feat(scan): add confidence + remediation + fingerprint to secrets (rc-23v) | ✅ Yes |
| #60 | [DRAFT/EXPERIMENTAL] feat: prompt-injection detector (rf-bmo) | ❌ No |

## PRs with Rome Comments

### PR #75 — feat(scan): add confidence + remediation + fingerprint to secrets

**Rome's comment (2026-04-30):** Audit summary identifying 3 internal consumer gaps.

**Gaps identified:**
1. `node/src/commands/issues/issue-builder.ts` (`buildFromLocalMatch`):
   - `LocalScanResult` interface omits `confidence`, `remediation`, `fingerprint`
   - Fingerprint format mismatch: 12-hex `sha256(file:rule)` vs scanner's 16-hex `sha256(file\0rule\0redacted)`
   - Hard-coded 3-step generic remediation overrides per-pattern strings

2. `node/src/commands/report.ts`: HTML report ignores all three new fields.

3. `node/src/commands/notify.ts`: Slack/Discord webhooks ignore all three fields.

**Follow-up beads filed:**
- `sable-6x1` (P1): Fix issue-builder.ts fingerprint format + per-pattern remediation → assigned to jasper
- `sable-wkp` (P1): Surface confidence/remediation/fingerprint in report.ts + notify.ts → assigned to obsidian

### PR #95 — feat(promo): agent-only 60s video production pipeline

**Rome's comment (2026-05-09):** Announced VO switch from ElevenLabs to HeyGen (commit 2b78d38). Documents HeyGen v2 video API integration with optional talking-head avatar bookend.

**Remaining agent-doable work:**
- Capture missing beat tapes: 06 commit graph, 07 split-screen, 10 founder cameos, 11 MCP demo, 13 dashboard
- Resolve `PENDING-FIRST-FETCH` SHAs in `promo/audio/sfx/manifest.json`
- Build fixture skill at `fixtures/skills/hostile-example/` for tape 09

**Human-gated (not beaded as agent tasks):**
- API keys in `promo/.env` (HEYGEN_AVATAR_ID, SUNO_API_KEY)
- End-to-end render review + public posting approval

**Follow-up bead filed:**
- `sable-qcv` (P2): Advance promo pipeline remaining tapes + SFX SHAs → assigned to quartz

## Status

Beads filed. Witness nudged to sling when polecats available (none active at time of scan).

Definition of done: ✅ Every open PR with a Rome comment has a corresponding follow-up bead and assigned owner.
